import Component from '@ember/component';

/*
  List of gh-insights types.

  Per User:
    1. Activity charts(gh-streaks)
      - by repo
      - by date
      - by organization
    2. Languages chart
      - by all repos
      - by dates
    3. Events
      - feed user events
      - feed organization events
      - feed events by my repo
 */

export default class GhInsightsComponent extends Component {
}
